
#search
  = render :partial => 'users/search'

%h1 All Users


= will_paginate
%ul.users
  -@users.each do |user|
    %li
      = image_tag("/mysteryman.jpg", :class => "gravatar")
      = link_to user.name, user
		
= will_paginate
	